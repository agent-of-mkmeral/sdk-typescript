/**
 * File read tool for reading files in multiple formats.
 *
 * Supports text, image, and document files with automatic format detection.
 */

export { fileRead } from './file-read.js'
export type { FileReadInput, FileReadMode } from './types.js'
